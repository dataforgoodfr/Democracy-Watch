{#
  Réponse HTMX de l'explorateur : le tableau, qui atterrit dans
  `hx-target="#amendement-results"`, suivi de la barre de contrôle réémise en
  swap « out of band ».

  Sans ce second fragment, une facette appliquée rafraîchissait les lignes mais
  laissait les pastilles et les boutons `+` dans leur état précédent : le tableau
  et la barre affichaient alors deux filtrages différents.
#}
{% include "partials/amendement_results.html.j2" %}
{% with oob = true %}{% include "partials/amendement_controls.html.j2" %}{% endwith %}
